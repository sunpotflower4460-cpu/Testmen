# Cloudflare Workers（静的アセット配信）設定
# ビルド成果物 dist/ をそのまま配信する。サーバーコード(main)は不要。
name = "testmen"
compatibility_date = "2025-01-01"

[assets]
directory = "./dist"
# 未知のパスは index.html を返す（単一ページアプリ向け・将来のルーティングにも安全）
not_found_handling = "single-page-application"
